\section{Related and Future Work}
\label{sec:related}

Paco, gpaco~\cite{paco,gpaco}.

Enhanced coinduction, the tower~\cite{pous-companion,schafer-smolka}.

\note{Also worth citing here: the diacritical companion~\cite{diacritical}, which
is the passive/active distinction the future-work item below builds on;
ctrees~\cite{ctrees} as the contrast case, a development built on enhanced
coinduction from the start rather than migrated; and Vellvm~\cite{vellvm} for the
leaking-abstraction argument in \Cref{sec:intro}. All are in \code{refs.bib}
already.}

\subsection{Future work}

Theoretical strengthening of the coinduction library: active up-to techniques.

\note{A second, much smaller future-work item, if one is wanted: deduplicating
the nineteen local tactic definitions in \code{extra/IForest.v}, which is what
stands between the whole development and finishing under the paco baseline on
tactic surface.}
